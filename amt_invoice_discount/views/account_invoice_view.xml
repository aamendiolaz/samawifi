<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="discount_account_invoice_view_form" model="ir.ui.view">
            <field name="name">discount.account.invoice</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='amount_untaxed']" position="after">
                        <field name="amount_discount" attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'out_receipt'))]}"/>
                </xpath>
                <xpath expr="//field[@name='narration']" position="after">
                    <div>
                        <label for="discount_type" string="Discount Type :" attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'out_receipt'))]}"/>
                        <field name="discount_type" class="oe_inline" attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'out_receipt'))]}"/>
                    </div>
                    <div>
                        <label for="discount_rate" string="Discount Value :" attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'out_receipt'))]}"/>
                        <field name="discount_rate" class="oe_inline" attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'out_receipt'))]}"/>
                    </div>
                </xpath>
            </field>
        </record>

    </data>
</odoo>
